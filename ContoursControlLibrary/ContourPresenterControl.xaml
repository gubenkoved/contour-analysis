<UserControl x:Class="ContoursControlLibrary.ContourPresenterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             Loaded="UserControl_Loaded" d:DesignHeight="112" d:DesignWidth="115"
             Name="Control">
    <UserControl.Resources>
        <Style x:Key="DefaultContourStyle">
            <Setter Property="Polyline.StrokeThickness" Value="1.0" />
            <Setter Property="Polyline.Stroke" Value="Black" />
            <Setter Property="Polyline.StrokeLineJoin" Value="Round" />
            <Setter Property="Control.Opacity" Value="1.0" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Canvas Name="ContourCanvas"/>                
    </Grid>
</UserControl>
